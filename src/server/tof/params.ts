type LookupByUidParams = {
  uid: string;
};

type LookupByNameParams = {
  name: string;
};

type ScanParams = {
  nickname: string;
};

export { LookupByUidParams, LookupByNameParams, ScanParams };
